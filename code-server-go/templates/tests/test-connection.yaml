apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "code-server-go.fullname" . }}-test-connection"
  labels:
    {{- include "code-server-go.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "code-server-go.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
